FROM cm2network/csgo:sourcemod

# TODO
# on start up.. last steps are

#compile whitelist.sp to whitelist.smx. copy to addons/sourcemod/plugins/
#create addons/sourcemod/configs/whitelist.txt. steamid per line

#compile retakes-enhanced.sp to *.smx. copy to addons/sourcemod/plugins/

# fetch https://github.com/anthonyliao/retakes-instadefuse/blob/master/scripting/retakes_instadefuse.sp to local
# compile to *.smx. copy to addons/sourcemod/plugins/

# copy *.cfg files into cfg/